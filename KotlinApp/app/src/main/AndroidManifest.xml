<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.leonardo.kotlinapp">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_garfo_faca"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_garfo_faca_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SobremesasApp">

        <activity android:name=".view.activities.AddUpdateDishActivity"
            android:label="Add Dish"
            android:theme="@style/Theme.SobremesasApp.NoActionBar"
            android:configChanges="orientation"></activity>

        <activity
            android:name=".view.activities.SplashActivity"
            android:theme="@style/Theme.SobremesasApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.activities.MainActivity"
            android:label="@string/app_name"></activity>
    </application>

</manifest>